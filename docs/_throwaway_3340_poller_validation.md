# Throwaway PR — system_3 #3340 poller validation

This PR exists only to validate that `/api/prs/operator_authored` and the
system_3 Overview panel surface a non-`wt/<role>/task-<id>` PR on this repo
after one refresh-cron poll tick. It will be closed without merging once
that is confirmed.
